{{#if reward.value}}
  {{lookup . 'reward.fuelTankType'}}
  {{> (stringFormat 'programs/%s/referral' reward.fuelTankType)}}
{{else}}
  {{> (stringFormat 'programs/%s/referral' programDetails.referredRewardDetails.fuelTankType)}}
{{/if}}
