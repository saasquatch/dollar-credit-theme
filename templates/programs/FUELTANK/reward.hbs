{{#if reward.value}}
  {{#if reward.fuelTankType}}
    {{> (stringFormat 'programs/%s/reward' reward.fuelTankType)}}
  {{else}}
    {{> (stringFormat 'programs/%s/reward' reward.type)}}
  {{/if}}
{{else}}
  {{> (stringFormat 'programs/%s/reward' programDetails.referredRewardDetails.fuelTankType)}}
{{/if}}
